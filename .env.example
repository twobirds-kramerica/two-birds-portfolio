# Environment variable template for Two Birds tooling.
# Copy to .env (already gitignored) OR set these as shell/user env vars.
# Never put real secrets in this file. Never commit a populated .env.

# --- Notion sync (hal-stack/notion-sync/) ------------------------------------
# Internal integration token from https://www.notion.so/my-integrations.
# See hal-stack/notion-sync/SETUP.md for the full setup procedure.
NOTION_API_KEY=secret_paste_your_notion_integration_token_here
