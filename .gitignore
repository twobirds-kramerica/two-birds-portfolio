# Environment files
.env
.env.local
.env.production

# Claude.ai data export — private conversation data, never push
hal-stack/context-system/ingestion/claude-export.zip
hal-stack/context-system/ingestion/raw/

# Python bytecode cache from hal-stack/notion-sync/ scripts
__pycache__/
*.pyc
*.pyo

# Local artifact scratch (e.g. axe-core report downloads for inspection)
.axe-tmp/

# Claude Code local-only settings (per-machine permission toggles)
.claude/settings.local.json

# Claude Code internal runtime dirs — reinstalled on demand, not source
.agents/
.claude/skills/

# Stale export duplicates (e.g. TTS plain-text copies)
*.md.txt
